name: "Run Tests"
description: "Runs all the tests"
runs:
  using: "composite"
  steps:
    - name: Run Tests
      env:
        MAVEN_OPTS: "-Xmx8g"
        MAVEN_CONFIG: ~/.m2/repository
      shell: bash
      run: |
        mvn -B -e verify -s ./settings.xml
